# Profile configuration
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Database configuration (will be overridden by profile-specific properties)
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# JPA/Hibernate configuration to prevent prepared statement conflicts
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Connection pool and statement caching settings
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false

# Security settings
jwt.secret=${JWT_SECRET:ishan_and_akole1419_very_long_secure_key_for_hs256_algorithm_security}
jwt.expiration=86400000

# Management endpoints
management.endpoints.web.exposure.include=*
management.endpoint.prometheus.enabled=true

# Default Categories Configuration
app.default-categories.enabled=true

# CORS configuration
app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,https://localhost:3000,http://127.0.0.1:3000}

# LangChain4j Google AI Gemini Configuration
langchain4j.google-ai-gemini.chat-model.api-key=AIzaSyB4BR7oKkA5mFw2U0hnt2Rbs2_FYeATPGU
langchain4j.google-ai-gemini.chat-model.model-name=gemini-2.5-flash
langchain4j.google-ai-gemini.chat-model.timeout=60s
langchain4j.google-ai-gemini.chat-model.temperature=0.7